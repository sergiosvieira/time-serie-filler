project(TimeSerieFiller)
cmake_minimum_required(VERSION 3.1)
add_definitions("-std=c++17")
include(${CMAKE_BINARY_DIR}/conanbuildinfo.cmake)
conan_basic_setup()
add_executable(timeserie main.cpp)
target_link_libraries(timeserie ${CONAN_LIBS})
